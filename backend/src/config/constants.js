const CATEGORIES = ["shoes", "clothing", "accessories", "bags"];
const SORT_FIELDS = ["createdAt", "price", "name"];
const SORT_ORDERS = ["asc", "desc"];

const DEFAULT_PAGE = 1;
const DEFAULT_LIMIT = 10;
const MAX_LIMIT = 100;

module.exports = {
  CATEGORIES,
  SORT_FIELDS,
  SORT_ORDERS,
  DEFAULT_PAGE,
  DEFAULT_LIMIT,
  MAX_LIMIT,
};
